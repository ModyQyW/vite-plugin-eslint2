// This file deliberately contains lint problems so the custom overlay has
// something to render. Edit it (remove/add problems) to see the overlay update.
const unused = "this triggers no-unused-vars";

// eslint-disable-next-line no-console
console.log("hello from custom-overlay demo", unused);
